<div class="card h-100 shadow-sm">
    <img src="{{ producto.imageLink }}" class="card-img-top" alt="{{ producto.name }}">

    <div class="card-body d-flex flex-column">
        <h5 class="card-title">{{ producto.name }}</h5>
        <p class="card-text text-muted">{{ producto.brand|default('Marca desconocida') }}</p>
        <p class="card-text text-truncate">{{ producto.description|default('Sin descripción') }}</p>
    </div>
</div>
