<nav class="navbar navbar-expand-lg bg-white shadow-sm">
    <div class="container">
        <a class="navbar-brand fw-semibold" href="{{ path('app_home') }}">
            <img src="{{ asset('img/icono-oscuro.png') }}" alt="Logo" height="45">
            MakeupReviews
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarMain">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    {# Vacío #}
                </li>
            </ul>

            <div class="d-flex gap-2">
                {% if app.user %}
                    <span class="align-self-center text-muted">Hola, {{ app.user.username}}</span>
                    <a href="{{ path('app_logout') }}" class="btn btn-outline-secondary">Cerrar sesión</a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="btn btn-outline-secondary">Iniciar Sesión</a>
                    <a href="{{ path('app_register') }}" class="btn btn-primary">Registrarse</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
